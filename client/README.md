# Grocery Frontend

An HTML/JS frontend that works with the Express backend.

Open `index.html` in a browser. It expects the backend to run on `http://localhost:3001` and allows adding/removing items from a cart.
